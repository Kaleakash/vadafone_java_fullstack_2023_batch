import { useState } from "react";

function ProductOperation() {
let [id,setId]=useState(0);     // only one value defalt is 0

// let [products,setProducts]=useState([
//     {pid:1,pname:"TV"},
//     {pid:2,pname:"Compouter"}
// ])

//let product = products.map(p=><li>PId {p.pid}</li>)

    return(
        <div>


        </div>
    )
}

export default ProductOperation;